import session from "./session.controller";
import user from "./user.controller";
import category from "./category.controller";
import realEstate from "./realEstate.controller";
import schedule from "./schedule.controller";

export default {
    schedule,
    user,
    session,
    category,
    realEstate
}